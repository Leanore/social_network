<%= stylesheet_link_tag "sign_in_form_styles" %>
<div class="sign_in_box">
<%= form_for(resource, :as => resource_name, :url => session_path(resource_name), :html => { :class => "form-signin" }) do |f| %>
  <h2 class="form-signin-heading">Please sign in</h2>   
  <div>
    <%= f.email_field :email, :class =>"input-block-level", :placeholder =>"Email address" %>
  </div>

  <div>
    <%= f.password_field :password, :class =>"input-block-level", :placeholder =>"Password" %>
  </div>

  <% if devise_mapping.rememberable? -%>
    <div class="checkbox"><%= f.check_box :remember_me %> <%= f.label :remember_me %></div>
  <% end -%>

  <div><%= f.submit "Sign in", :class => 'btn btn-large btn-primary' %></div>
  
  <div class="dev_links"><%= render "devise/shared/links" %></div>
<% end %>
</div>

